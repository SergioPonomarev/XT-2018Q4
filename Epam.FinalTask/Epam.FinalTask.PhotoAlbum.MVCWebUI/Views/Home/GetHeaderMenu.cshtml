<nav class="navbar navbar-expand-lg navbar-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Taggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                @Html.ActionLink("Home", "Index", "Home", null, new { @class = "nav-link" })
            </li>
            @if (User.IsInRole("Users"))
            {
                <li class="nav-item">
                    @Html.ActionLink("Upload", "UploadImage", "Image", null, new { @class = "nav-link" })
                </li>
            }

            @if (User.IsInRole("SuperAdmins"))
            {
                <li class="nav-item">
                    @Html.ActionLink("Management", "Manage", "Administration", null, new { @class = "nav-link" })
                </li>
            }
        </ul>
    </div>
</nav>